services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-geolead}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-local-postgres-password-change-me}
      POSTGRES_DB: ${POSTGRES_DB:-geolead}
    ports:
      - "127.0.0.1:${POSTGRES_HOST_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./src/migrations:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U $${POSTGRES_USER:-geolead} -d $${POSTGRES_DB:-geolead}",
        ]
      interval: 5s
      timeout: 5s
      retries: 10

  mongo:
    image: mongo:7
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_INITDB_ROOT_USERNAME:-geolead}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_INITDB_ROOT_PASSWORD:-local-mongo-password-change-me}
      MONGO_INITDB_DATABASE: ${MONGO_INITDB_DATABASE:-geolead}
    ports:
      - "127.0.0.1:${MONGO_HOST_PORT:-27017}:27017"
    volumes:
      - mongo_data:/data/db
    healthcheck:
      test:
        [
          "CMD",
          "mongosh",
          "--quiet",
          "-u",
          "${MONGO_INITDB_ROOT_USERNAME:-geolead}",
          "-p",
          "${MONGO_INITDB_ROOT_PASSWORD:-local-mongo-password-change-me}",
          "--authenticationDatabase",
          "admin",
          "--eval",
          "db.adminCommand('ping').ok",
        ]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    command: >
      redis-server
      --appendonly no
      --maxmemory 256mb
      --maxmemory-policy allkeys-lru
    ports:
      - "127.0.0.1:${REDIS_HOST_PORT:-6379}:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  engine:
    build:
      context: .
      dockerfile: Dockerfile.engine
    ports:
      - "127.0.0.1:${ENGINE_HOST_PORT:-8000}:8000"
    volumes:
      - engine_models:/home/appuser/app/models
    environment:
      ENVIRONMENT: production
      HOST: 0.0.0.0
      PORT: 8000
      LOG_LEVEL: info
      METRICS_ENABLED: "true"
      REQUEST_ID_HEADER: x-request-id
      MODEL_VERSION: "0.1.0"
      INTERNAL_API_KEY: ${INTERNAL_API_KEY:-local-internal-api-key-change-me-32}
      EMBEDDINGS_ENABLED: ${EMBEDDINGS_ENABLED:-false}
      EMBEDDINGS_PROVIDER: ${EMBEDDINGS_PROVIDER:-api}
      EMBEDDINGS_MODEL: ${EMBEDDINGS_MODEL:-text-embedding-3-small}
      HOOKS_LLM_ENABLED: ${HOOKS_LLM_ENABLED:-false}
      RETRAIN_MODE: ${RETRAIN_MODE:-inline}
      CELERY_BROKER_URL: ${CELERY_BROKER_URL:-}
      CELERY_RESULT_BACKEND: ${CELERY_RESULT_BACKEND:-}
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=2)",
        ]
      interval: 5s
      timeout: 3s
      retries: 10

  engine-worker:
    profiles: ["worker"]
    build:
      context: .
      dockerfile: Dockerfile.engine
    command: >
      celery
      -A app.ml.tasks:celery_app
      worker
      --loglevel=info
    environment:
      ENVIRONMENT: production
      LOG_LEVEL: info
      MODEL_VERSION: "0.1.0"
      INTERNAL_API_KEY: ${INTERNAL_API_KEY:-local-internal-api-key-change-me-32}
      MODELS_DIR: models
      RETRAIN_MODE: celery
      CELERY_BROKER_URL: ${CELERY_BROKER_URL:-redis://redis:6379/0}
      CELERY_RESULT_BACKEND: ${CELERY_RESULT_BACKEND:-redis://redis:6379/1}
    volumes:
      - engine_models:/home/appuser/app/models
    depends_on:
      redis:
        condition: service_healthy

  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${APP_HOST_PORT:-3000}:3000"
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: 3000
      LOG_LEVEL: info
      JWT_SECRET: ${JWT_SECRET:-local-jwt-secret-change-me-with-at-least-32-chars}
      JWT_EXPIRES_IN: 15m
      CORS_ORIGIN: ""
      RATE_LIMIT_MAX: "100"
      RATE_LIMIT_WINDOW: 1 minute
      LOGIN_RATE_LIMIT_MAX: "5"
      BODY_LIMIT: "1048576"
      SWAGGER_ENABLED: "true"
      DB_ENABLED: "true"
      POSTGRES_URL: postgresql://${POSTGRES_USER:-geolead}:${POSTGRES_PASSWORD:-local-postgres-password-change-me}@postgres:5432/${POSTGRES_DB:-geolead}
      POSTGRES_POOL_MAX: "10"
      POSTGRES_SSL: "false"
      MONGO_URL: mongodb://${MONGO_INITDB_ROOT_USERNAME:-geolead}:${MONGO_INITDB_ROOT_PASSWORD:-local-mongo-password-change-me}@mongo:27017/${MONGO_INITDB_DATABASE:-geolead}?authSource=admin
      REDIS_URL: redis://redis:6379
      CACHE_TTL_SECONDS: "3600"
      METRICS_ENABLED: "true"
      TRACING_ENABLED: ${TRACING_ENABLED:-false}
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-http://jaeger:4318/v1/traces}
      MOTOR_URL: ${MOTOR_URL:-http://engine:8000}
      MOTOR_API_KEY: ${MOTOR_API_KEY:-local-internal-api-key-change-me-32}
      MOTOR_TIMEOUT_MS: "10000"
      ADMIN_EMAIL: ${ADMIN_EMAIL:-}
      ADMIN_PASSWORD_HASH: ${ADMIN_PASSWORD_HASH:-}
      EMBEDDINGS_ENABLED: ${EMBEDDINGS_ENABLED:-false}
      EMBEDDINGS_PROVIDER: ${EMBEDDINGS_PROVIDER:-api}
      EMBEDDINGS_MODEL: ${EMBEDDINGS_MODEL:-text-embedding-3-small}
      HOOKS_LLM_ENABLED: ${HOOKS_LLM_ENABLED:-false}
      RETRAIN_MODE: ${RETRAIN_MODE:-inline}
      CELERY_BROKER_URL: ${CELERY_BROKER_URL:-redis://redis:6379/0}
      CELERY_RESULT_BACKEND: ${CELERY_RESULT_BACKEND:-redis://redis:6379/1}
      CNPJ_API_URL: ${CNPJ_API_URL:-}
      CNPJ_API_KEY: ${CNPJ_API_KEY:-}
    depends_on:
      postgres:
        condition: service_healthy
      mongo:
        condition: service_healthy
      redis:
        condition: service_healthy
      engine:
        condition: service_healthy

volumes:
  postgres_data:
  mongo_data:
  engine_models:
